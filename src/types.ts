export type RatingData = {
  rating: number;
  title: string;
  comment: string;
  imgUrl: string;
  name: string;
  occupation: string;
};

export type ClassNameOverride = { className: string } | {};

export type AccordionItem = {
  title: string;
  content: string;
};
